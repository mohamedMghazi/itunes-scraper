# Server Configuration
PORT=3001 # (default)
NODE_ENV=development

# Database Configuration
# Replace with your MongoDB connection string
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# External APIs
ITUNES_API_URL=https://itunes.apple.com

# CORS Configuration
# Comma-separated list of allowed domains (supports regex patterns)
# Examples:
#   - http://localhost - allows localhost on any port
#   - https://example.com - allows exact domain
#   - https://*.example.com - allows all subdomains
ALLOWED_ORIGINS=http://localhost,https://domain-name.com

# Optional: Logging Level
# Options: debug, info, warn, error
LOG_LEVEL=info
